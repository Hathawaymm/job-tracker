# AI 求职助手 · 用户操作手册

> 本地全栈求职工具：多版本简历 + 岗位自动抓取 + AI 匹配/话术/模拟面试 + 投递追踪。
> 所有数据保存在本机，仅与本地服务通信，不上传任何内容。

---

# 第一部分 · 快速上手（约 5 分钟）

## ① 安装并启动

```bash
cd ~/Desktop/opencode-test/job-tracker
npm install          # 首次安装（含 client/server 依赖）
npm run dev          # 同时启动 服务端:3001 + 前端:5173
```

浏览器打开 **http://localhost:5173** 即可使用。

> 扩展抓取需要单独装一次依赖：`cd extension && npm install`（见第 ③ 步）。

## ② 先建「经历库」（建议但可后补）

点击顶部 **「经历库」** tab → 填「项目名称」等 → 点 **「保存到经历库」**。
把简历里的项目经历都录进来，后续「针对性简历 / AI 生成简历」会从这里挑素材。

## ③ 安装 Chrome 抓取扩展（自动抓取岗位用）

1. Chrome 地址栏打开 `chrome://extensions`
2. 右上角开启「开发者模式」
3. 点「加载已解压的扩展程序」，选择本项目的 **`extension/install-this`** 目录
4. 回页面点 **「刷新状态」**，抓取扩展显示 **🟢 在线** 即成功

## ④ 简历

点击 **「简历」** tab：
- 点「📄 上传导入」上传你的 Word/PDF/Markdown/图片，AI 自动提取；或点「+ 新建」手填
- 字段直接点击即可编辑，编辑完 **必须点「💾 保存简历」**（不会自动保存）
- 支持多个简历版本，顶部下拉切换，各功能页可选用不同版本

## ⑤ 抓取岗位 + AI 筛选

**岗位库** tab：
- 上半区「🤖 自动抓取」：填「岗位名称」（如 项目经理）、城市、薪资，点 **「⚡ 立即抓取」**，等 1-2 分钟
- 匹配度达阈值的岗位出现在 **「待确认池」**，点 **「✅ 确认投递」** 纳入投递流程
- 下半区「手动管理」：可 **「+ 新增岗位」** 手录，或 **「🔗 粘贴链接抓取」**，再点 **「🤖 AI 筛选」** 给每个岗位打分

## ⑥ 生成定制招呼语

**招呼语** tab → 对已确认的岗位点 **「✨ 生成定制招呼语」**。
投递节奏：BOSS 里先发简历图片 → 再发这段话术 → 点 **「✅ 已投递（进清单）」**。

## ⑦ 投递追踪

**投递清单** tab：按阶段看进度，每个岗位可「➕ 添加面试记录」；投递后 3 天未更新会有跟进提醒。

## ⑧ 模拟面试

**模拟面试** tab → 选好简历 → 点 **「🎬 开始面试」**，面试官结合你的简历提问，答完后点 **「📋 结束并 AI 复盘」**。

---

## 功能导航速查表

| 我想做什么 | 去哪个页 | 点什么 |
|---|---|---|
| 录入项目经历 | 经历库 | 表单 → 保存到经历库 |
| 导入简历 | 简历 | 📄 上传导入 |
| 生成针对性简历 | 岗位库 | 岗位卡片「📄 针对性简历」 |
| 自动抓岗位 | 岗位库 | ⚡ 立即抓取（需扩展在线） |
| AI 打分 | 岗位库 | 🤖 AI 筛选 |
| 定制招呼语 | 招呼语 | ✨ 生成定制招呼语 |
| 查投递进度 | 投递清单 | 阶段 tab |
| 练面试 | 模拟面试 | 🎬 开始面试 |
| 备份数据 | 任意页顶部 | 导出备份 / 导入 |

---

# 第二部分 · 完整详尽操作手册

## 第 1 章 简介

**AI 求职助手** 是一套本地运行的求职工作台，把「筛岗 → 写简历 → 打招呼语 → 投递 → 跟进 → 面试」串成一条流水线：

- **多版本简历**：可保存多个简历版本（通用版 / 定向版），编辑、预览、导出 Word/图片/PDF
- **岗位自动抓取**：通过 Chrome 扩展复用你的猎聘/BOSS 登录态，按关键词定时抓取
- **AI 匹配**：结合简历给每个岗位打分（高匹配 / 可尝试 / 低匹配 / 建议拒绝）
- **千岗千面招呼语**：针对每个岗位 JD 定制开场白
- **投递追踪**：阶段化跟进 + 面试记录 + 操作日志
- **模拟面试**：AI 面试官结合你的简历提问追问，结束后一键复盘

架构：`client`（前端网页）＋ `server`（本地 Express + SQLite）＋ `extension`（Chrome 抓取扩展）。

---

## 第 2 章 安装与启动

### 2.1 首次运行

```bash
cd ~/Desktop/opencode-test/job-tracker
npm install        # 安装 client 与 server 依赖
npm run dev        # 同时启动服务端(:3001)与前端(:5173)
```

扩展为独立项目，需单独安装依赖：

```bash
cd extension && npm install
```

### 2.2 打开界面

浏览器访问 **http://localhost:5173**。顶栏「DeepSeek V4-Flash」「GLM-4.6V 识图」两个状态点：
- **绿点**：该 AI 能力可用
- **红点/无点**：服务未启动或配置异常（AI key 从系统目录读取，见第 9 章）

### 2.3 安装 Chrome 抓取扩展

抓取岗位依赖浏览器扩展（复用你的猎聘/BOSS 登录态）：

1. 打开 `chrome://extensions`
2. 右上角开启「开发者模式」
3. 点「加载已解压的扩展程序」→ 选择本项目的 **`extension/install-this`** 目录
4. 回到页面点「刷新状态」，扩展显示 **🟢 在线** 即可抓取

> 修改扩展源码后需重新构建：`cd extension && npm run release`，再到 Chrome 重新加载。

### 2.4 备份与恢复

顶部右侧 **「导出备份」** 把全部数据下载为 JSON 文件；**「导入」** 选择备份文件可恢复。
导入会**覆盖当前数据**，操作前会再次确认。

---

## 第 3 章 经历库

> 用途：集中管理你的项目经历，作为生成「针对性简历 / AI 生成简历」的素材库。持久化在本地 SQLite，跨浏览器/设备不丢失。

### 3.1 新增项目经历

「项目名称 *」必填，其余可选：

| 字段 | 说明 | 示例 |
|---|---|---|
| 项目名称 * | 必填 | 订单中心自研重构 |
| 公司 | 所属公司/单位 | 路特创新科技 |
| 担任角色 | 你在项目中的角色 | 项目经理 |
| 时间 | 起止时间 | 2024.01 - 2024.12 |
| 项目简介 | 一句话背景与目标 | — |
| 项目要点 | 每行一条，**建议含量化结果** | 订单处理从 5 分钟/单缩至 1 分钟内 |
| 标签 | 逗号分隔，供 AI 匹配用 | 电商、供应链、EDI |

填好后点 **「保存到经历库」**。列表中的条目**点击字段即就地编辑**（自动保存，失败自动还原），「删除」需确认。

### 3.2 从简历导入项目

「📥 从简历导入项目」：把当前选中简历版本的「项目经历」导入经历库，按 **公司+名称** 自动去重，已存在则跳过。

### 3.3 从文件导入

「📂 从文件导入」支持：
- **Word（.docx）/ PDF / Markdown / 图片**：AI 自动识别其中的项目经历
- **JSON**：经历数组，或 `{"items":[...]}` 结构

导入按 **公司+名称** 去重，跳过已存在的条目。

---

## 第 4 章 简历

### 4.1 版本管理

顶部「简历版本」下拉可切换已有版本；按钮：

| 按钮 | 作用 |
|---|---|
| + 新建 | 建空白简历（弹窗命名） |
| 📄 上传导入 | 上传 Word/PDF/Markdown/图片，AI 提取并生成新版本 |
| 复制 | 复制当前版本为新版本 |
| 删除 | 删除当前版本（保留至少一个） |
| 💾 保存简历 | **手动保存**当前编辑内容（见下） |
| 👁 预览 | 预览 + 导出（图片/Word/PDF） |
| 🔍 AI 诊断 | 对简历做亮点/不足/改进建议分析 |
| ✨ AI 生成简历 | 用经历库 + 目标岗位 JD 生成定制版本 |

> ⚠️ **重要**：简历为**手动保存**，编辑字段后必须点「💾 保存简历」，否则刷新会丢失。

### 4.2 编辑方式

点击任意字段（姓名/求职意向/技能/经历/简介等）即可进入编辑，失焦保存到当前编辑态。区块均可增删条目：工作经历「+ 添加」、项目经历「+ 添加」、教育经历「+ 添加」。

### 4.3 AI 诊断与优化

1. 点 **「🔍 AI 诊断」**，下方展示「亮点 / 不足 / 改进建议」
2. 满意后点 **「✨ 基于诊断生成优化简历」**：生成新版本「AI-原版本名」并自动切换，原版本保留可对比

### 4.4 AI 生成简历（推荐用于定向投递）

点 **「✨ AI 生成简历」** → 弹出「选择目标岗位 JD」：
- **从岗位库选择**：列出现有岗位，选一个作为目标 JD
- **手动粘贴 JD**：粘贴任意岗位描述

生成逻辑：从**个人经历库**挑选最匹配的项目 + 结合当前简历，工作/教育/基础信息自动继承当前版本，生成新版本「AI-岗位-公司-日期」。生成后可直接编辑再保存。

### 4.5 项目经历：从经历库选填

项目模块：
- 顶部 **「📥 从经历库选择」**：从经历库选一条，作为**新条目**插入（带完整数据）
- 每条项目右上角 **「📥 选填」**：用经历库条目**覆盖**当前项目内容（已有内容会二次确认）
- 填充后即普通条目，可继续编辑；**不会回写经历库**

### 4.6 一键 STAR 润色

每个项目卡片右下 **「✨ 一键 STAR 润色」**：AI 按 STAR 法则重写「项目要点」。润色前自动保留原版本，可点 **「↩ 回退」** 还原。

### 4.7 预览与导出

「👁 预览」弹层中：
- **🖼 导出图片**：整页高清 PNG
- **📝 导出 Word**：生成 .docx（本地生成，不耗 AI token）
- **📄 导出 PDF**：走浏览器打印另存为 PDF
- ESC 或点空白处关闭

---

## 第 5 章 岗位库

### 5.1 自动抓取面板（🤖 自动抓取）

**前提**：Chrome 扩展在线（🟢）；默认平台为猎聘（免登录），BOSS 直聘需登录态。

| 配置项 | 说明 |
|---|---|
| 抓取平台 * | 猎聘（默认）/ BOSS直聘 |
| 岗位名称 * | 如「项目经理」 |
| 城市 | 如「深圳」（留空 = 全国） |
| 薪资范围 | 支持「月薪 k」（20k-30k）或「年薪 万」（30-50万） |
| 单次抓取上限 | 1-50 条 |
| 匹配度阈值 % | 默认 65；AI 评分 ≥ 该值才进待确认池（0-100） |
| 目标行业 | 多选标签，用于 AI 评分排序锚定：行业不匹配自动降分；**不影响搜索/过滤**。输入回车新增，✕ 删除 |

操作按钮：
- **⚡ 立即抓取**：立即执行一次，期间显示实时进度（抓取中→评分→完成）
- **保存搜索设置**：把以上配置 + 当前简历同步到服务器
- **刷新状态**：重新加载配置与扩展状态
- **开启定时抓取**：每天 3 个时段（上午 8-9 / 中午 12-14 / 晚上 18-19）各随机时刻抓一次

底部「今日窗口」显示各时段是否已执行、上次抓取时间；若某日新增不足 3 条，自动跳过后续时段（次日恢复）。

> 合规约束：随机延迟 3-8 秒、单次最多 50 条、绝不自动投递。

### 5.2 待确认池（AI 推荐）

匹配度达阈值的岗位出现在「待确认池（AI 推荐 · 匹配度 X% · N 条）」：

| 操作 | 作用 |
|---|---|
| ✅ 确认投递 | 转入投递流程（岗位库 + 招呼语 + 投递清单） |
| 忽略 | 移出待确认池 |
| 🔗 查看岗位 | 打开原岗位链接 |
| 查看 JD | 展开完整 JD |

### 5.3 手动管理

- **+ 新增岗位**：手录岗位（公司/职位必填，JD 可粘贴或 **🖼 截图识图** 从图片提取）
- **🔗 粘贴链接抓取**：粘贴 BOSS/猎聘等详情页链接，经扩展抓取 JD 自动录入
- **🤖 AI 筛选全部未筛岗位**：给所有还没有评分的岗位批量打分
- 顶部统计：共 N 个岗位 · 未筛 N · 已确认 N

每个岗位卡片：

| 按钮 | 作用 |
|---|---|
| 🤖 AI 筛选 | 单岗评分（需 JD 与简历） |
| 📄 针对性简历 | 从经历库挑项目生成该岗位的定向简历版本「AI-岗位-公司」，成功后顶部出现「查看已生成的简历并编辑 →」链接跳转编辑 |
| ✅ 确认投递 | 确认后进入招呼语/投递流程 |
| 详情 / 收起 | 展开查看完整 JD 与备注 |
| 编辑 / 删除 | 修改 / 删除岗位 |

筛选 tab：全部 / 未筛选 / 高匹配 / 可尝试 / 低匹配 / 建议拒绝（带数量）。

> **去重保护**：新增或链接抓取时按公司+职位+链接自动判重，重复岗位会拦截提示。

---

## 第 6 章 招呼语

> 仅显示「已确认待投递」且未投递的岗位。顶部有**投递节奏建议**：BOSS 打开岗位 → 先发简历图片 → 再发定制招呼语（不刷屏不群发）→ 标记已投递，单岗闭环再投下一个。

| 操作 | 作用 |
|---|---|
| ✨ 生成定制招呼语 | 基于岗位 JD + 所选简历亮点生成（显示字数） |
| 🔄 重新生成 | 换一版 |
| 直接编辑 | 招呼语文本框可改，实时保存 |
| ✅ 已投递（进清单） | 标记已投，进入投递清单跟进 |

---

## 第 7 章 投递清单

### 7.1 总览

顶部展示各阶段数量：待确认 / **已确认待投递** / 话术就绪 / 已投递 / 进行中 / 已结束，下方按阶段 tab 过滤列表。

### 7.2 投递前简历图核对

**🖼 上传简历图核对**：上传你要发给对方的简历图片，AI（GLM-4.6V 识图）确认是否为简历并简述姓名/求职意向/核心经历，避免发错文件。

### 7.3 跟进与状态

- **待跟进提醒**：已投递 3 天以上未更新的岗位高亮提示，点「已跟进」清除
- **状态下拉**：手动更新（进行中/已结束等）
- **标记已投**：一键记录投递时间
- 卡片内展示话术内容、投递时间、最近更新时间

### 7.4 面试记录

每岗可 **「+ 添加面试记录」**：轮次（一面/二面/三面/技术面/HR面/笔试/其他）、日期、结果（待定/通过/未通过等）、面试内容、复盘。记录会按岗位归档展示。

### 7.5 操作日志

页面底部「操作日志（最近 50 条）」：录入/筛选/确认/投递等全部动作的时间线留痕。

---

## 第 8 章 模拟面试

- **🎬 开始面试**：AI 面试官基于**所选简历版本**开场并提问（首次会提示，若已有会话需确认清空）
- **输入回答**：下方文本框输入，Enter 发送（Shift+Enter 换行）
- **📋 结束并 AI 复盘**：对整场对话输出复盘（亮点 / 薄弱环节 / 改进建议含示例）
- **清空对话** / 切换简历版本会清空当前会话

> 面试为流式输出，基于你选择的简历提问并追问，建议真实作答以获得有效复盘。

---

## 第 9 章 数据与隐私

| 数据 | 位置 | 说明 |
|---|---|---|
| 简历 / 岗位 / 投递 / 日志 | 浏览器 localStorage（前缀 `jobtracker:`） | 简历需手动保存 |
| 个人经历库 | 本地 SQLite（`experience_bank` 表） | 跨浏览器不丢 |
| 抓取岗位 / 任务 / 抓取日志 | 本地 SQLite（`server/data/jobtracker.db`） | server 独占 |
| AI 密钥 | 系统目录 `~/.local/share/opencode/auth.json` | 不写入项目、不入 git |
| 运行日志 | `server/data/logs/jobtracker-日期.log` | 排查用 |

- 前端仅与本地服务 **127.0.0.1:3001** 通信，抓取扩展仅与本地通信，**不上传任何数据**
- 备份：顶部「导出备份」可随时导出全量数据

---

## 第 10 章 常见问题（FAQ）

**Q1：岗位库显示「抓取扩展 ⚪ 离线」？**
- 电脑休眠 / Chrome 关闭时离线属正常，唤醒后自动恢复（状态每 10 秒自动刷新）
- 若长期离线：确认已按 2.3 加载 `extension/install-this`，点「刷新状态」

**Q2：电脑休眠会不会错过定时抓取？**
- 已做双重保障：macOS 会在抓取窗口前自动唤醒电脑（需接电源），且唤醒后自动补跑错过的时段

**Q3：AI 筛选提示「该岗位还没有 JD 内容」？**
- 筛选和针对性简历依赖 JD。补充方式：编辑岗位粘贴 JD，或点「🖼 截图识图」从 JD 截图提取，或用「🔗 粘贴链接抓取」

**Q4：新增岗位提示「与已有岗位重复，已自动拦截」？**
- 去重保护生效：同一公司/职位/链接已存在。若确需重复录入，先删除旧岗位

**Q5：抓取结果很少 / 提示「已跳过后续时段」？**
- 可能匹配度阈值偏高或市场机会少。可调低「匹配度阈值」，或更换关键词/城市；跳过时段次日自动恢复

**Q6：AI 生成简历提示「个人经历库为空」？**
- 先到「经历库」录入项目经历（或用「从简历导入 / 从文件导入」），再生成

**Q7：改了扩展源码后抓取没变化？**
- 扩展需重新构建并加载：`cd extension && npm run release`，再到 `chrome://extensions` 重新加载

**Q8：数据会丢吗？**
- 简历需手动保存；抓取岗位/经历库在本地 SQLite。建议定期顶部「导出备份」，或先备份再执行破坏性操作
