{
  "name": "job-tracker-extension",
  "description": "AI 求职助手抓取扩展：配合本地服务在 BOSS直聘后台抓取岗位（复用你的登录态）",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "release": "wxt build && rm -rf install-this && mkdir -p install-this && cp -R .output/chrome-mv3/. install-this/",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wxt": "latest"
  }
}
